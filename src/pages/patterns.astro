---
import { getCollection } from "astro:content";
import { Picture } from "astro:assets";
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";
import Sectionhead from "@/components/sectionhead.astro";
import DownloadIcon from '@/components/ui/icons/download.astro';

// Filter patterns entries with 'draft: false' & date before current date
const publishedPatternsEntries = await getCollection("patterns", ({ data }) => {
  return !data.draft
});

// Sort content entries by publication date
publishedPatternsEntries.sort(function (a, b) {
  return b.data.publishDate.valueOf() - a.data.publishDate.valueOf();
});
---

<Layout title="Patterns">
  <Container>
    <Sectionhead>
      <Fragment slot="title">Printable Patterns</Fragment>
      <Fragment slot="desc">
        Download free patterns for coloring
      </Fragment>
    </Sectionhead>
    <main class="mt-16">
      <ul class="grid gap-8 max-w-4xl mx-auto">
        {
          publishedPatternsEntries.map((patternsPostEntry, index) => (
            <li>
              <a href={`/patterns/${patternsPostEntry.slug}`} class="block bg-white border-3 border-black p-6 shadow-[4px_4px_0px_#000] hover:shadow-[2px_2px_0px_#000] hover:translate-x-[2px] hover:translate-y-[2px] transition-all duration-150">
                <div class="grid md:grid-cols-2 gap-5 md:gap-10 items-center">
                  <div class="relative">
                    <Picture
                      src={patternsPostEntry.data.image.src}
                      alt={patternsPostEntry.data.image.alt}
                      sizes="(max-width: 800px) 100vw, 800px"
                      width={800}
                      height={600}
                      loading={index <= 2 ? "eager" : "lazy"}
                      decoding={index <= 2 ? "sync" : "async"}
                      class="w-full object-cover object-center bg-white border-3 border-black"
                    />
                    <div class="absolute top-4 right-4 bg-[var(--color-sunny)] border-2 border-black p-2">
                      <DownloadIcon />
                    </div>
                  </div>
                  <div>
                    <span class="bg-[var(--color-sky)] text-black border-2 border-black px-2 py-0.5 text-xs font-black uppercase tracking-wider">
                      {patternsPostEntry.data.category}
                    </span>

                    <h2 class="text-3xl font-black leading-snug tracking-tight mt-2 text-black">
                      {patternsPostEntry.data.title}
                    </h2>

                    <div class="flex gap-2 mt-3">
                      <span class="text-black font-medium">
                        {patternsPostEntry.data.author}
                      </span>
                    </div>
                  </div>
                </div>
              </a>
            </li>
          ))
        }
      </ul>
    </main>
  </Container>
</Layout>

<style>
.pattern-preview-img {
  position: relative;
}
.pattern-download-icon {
  position: absolute;
  top: 1rem;
  right: 1rem;
  background: rgba(255,255,255,0.7);
  border-radius: 50%;
  padding: 0.25rem;
  box-shadow: 0 2px 8px rgba(120, 80, 180, 0.10);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2;
}
.grid.md\:grid-cols-2 {
  position: relative;
}
.visually-hidden {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  white-space: nowrap !important;
  border: 0 !important;
  display: none !important;
}
</style>
